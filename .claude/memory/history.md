Last updated: 2026-08-26

# omniauth-hubspot-1

## Sources queried

- Local clone `/Users/braemel.thorn/Documents/claude/repos/omniauth-hubspot-1`: `git rev-parse --is-shallow-repository` → `false`; `git fetch origin` run before reading, so `origin/HEAD` is current.
- `git log origin/HEAD --oneline` → 21 commits (confirmed count matches the task brief).
- `git log origin/HEAD --format='...' --date=iso` (full bodies) → read all 21 commit messages in full.
- `git show --stat 6a9f511` (initial commit) → 4 files, 85 lines.
- `git show origin/HEAD:README.md`, `:omniauth-hubspot.gemspec`, `:entity.datadog.yaml`, `:lib/omniauth/strategies/hubspot.rb`, `:Gemfile`, `:CODEOWNERS`, `:.github/dependabot.yml`, `:.github/workflows/cleaner.yml` → all read in full at `origin/HEAD`.
- `git show 6a9f511:README.md` → initial README, compared line-for-line against current README (identical).
- `git ls-tree -r --name-only origin/HEAD` → 15 files, full tree read.
- `git shortlog -sne origin/HEAD` → 7 raw identities.
- `git log origin/HEAD -- .github/CODEOWNERS CODEOWNERS` → 2 commits.
- `git tag --sort=-creatordate` → 0 tags. `gh release list --repo unbounce/omniauth-hubspot-1` → 0 releases.
- `git merge-base --is-ancestor b5420f0 origin/HEAD` → `NO`; `git branch -a --contains b5420f0` → confirmed `b5420f0` (an oauth2-dependency-removal commit) sits only on unmerged branch `cleaner/unused-dependencies-oauth2-9ad99239eb`, not on `origin/HEAD`.
- `gh repo view unbounce/omniauth-hubspot-1 --json isFork,parent,description,repositoryTopics,createdAt,pushedAt,visibility,isArchived,defaultBranchRef,homepageUrl` → full metadata read.
- `gh repo view unbounce/omniauth-hubspot --json name,isArchived,createdAt` → `GraphQL: Could not resolve to a Repository`.
- `gh repo view chamaeleonidae/omniauth-hubspot --json name,isArchived,createdAt,owner` → resolves, non-archived, `createdAt: 2017-06-08T23:51:07Z`.
- `gh pr list --repo unbounce/omniauth-hubspot-1 --state all --json number,title,author,createdAt,mergedAt,closedAt,state --limit 50` → 9 PRs returned (page not full; total confirmed).
- `gh pr view 1`, `gh pr view 8`, `gh pr view 9` (`--json title,body,mergedAt,author`) → 3 PR bodies read in full.
- `gh run list --repo unbounce/omniauth-hubspot-1 --limit 30 --json databaseId,name,status,conclusion,createdAt` → 30 rows returned (retention-bounded as of 2026-08-26; oldest surviving run in this window is 2026-05-12).
- `gh workflow list --repo unbounce/omniauth-hubspot-1` → 3 workflows (Cleaner, reviewer.yml, Dependabot Updates).
- `gh api orgs/unbounce/teams/platform-services --jq .slug` → `platform-services` (verifies CODEOWNERS team slug resolves).
- Cross-repo consumer search: `git -C <repo> grep -lE 'omniauth-hubspot' origin/HEAD` run against all 234 non-excluded, non-subject clones under `/Users/braemel.thorn/Documents/claude/repos/` (237 total directories minus `lp-deployer`, minus the duplicate `ub-tf-infrastructure-ps4844`, minus the subject repo itself = 234; loop completed with zero `ERROR` exits recorded). 4 repos matched: `agentic-tools`, `golden-path`, `lp-webapp`, `paulw`. All 10 matching files and all 15 matching lines across those 4 repos were read and are enumerated below.
- `git -C <repo> grep -nE 'omniauth-hubspot' origin/HEAD` run against each of those 4 matched repos → 15 matching lines in 10 files: `agentic-tools` 2 lines in 2 files, `golden-path` 4 lines in 3 files, `lp-webapp` 8 lines in 4 files, `paulw` 1 line in 1 file.
- Jira JQL `text ~ "omniauth-hubspot"` (cloudId `fa5d0686-1e03-4c75-b8e1-c6d0a3226e01`) → 6 issues, page not capped.
- Jira JQL `summary ~ "omniauth-hubspot" OR summary ~ "hubspot omniauth"` → 0 issues.
- Jira JQL `text ~ "omniauth-hubspot-1"` → 2 issues (`PS-4701`, `PS-4167`), neither substantively about this repo's origin (both are org-wide rollout/standardization tickets that list this repo among many).
- Confluence CQL `text ~ "omniauth-hubspot"` limit 40 → 4 pages, page not capped.
- Confluence CQL `title ~ "omniauth-hubspot" OR title ~ "hubspot"` limit 40 → 33 results (`totalCount: 33`, below the 40 cap, so not page-limited). All 33 titles were read; none contains `omniauth`, and none names this repo or the `omniauth-hubspot` gem. The set is heterogeneous rather than confined to HubSpot the product/CRM: it includes the two 2019 space-`EN` incident/postmortem pages that record HubSpot/Constant Contact authentication breaking in `lp-webapp` (`2019-02-28 Cannot Create New HubSpot and Constant Contact Integrations`, id 195465310, and `2019-03-06 HubSpot and Constant Contact Post Mortem`, id 196150989 — the same two pages carried under Current state signals below), marketing and partnership material (`[Partnerships] HubSpot Email Swap Brief - Nov '18`, space REV; `2016-03-02 Hubspot Email Workshop`, space Creative), data-team competitive analysis (`Hubspot Competitive Analysis - AI Features` with its `Hubspot - AI Roadmap.pdf` attachment, and `[ARCHIVED] Data ProductsFY16Exp000-Marketing Emails-Exp000-Hubspot vs Custom`, all space DT), and 12 UI screenshots attached to one space-`BD` page (id 3452993554). The one on-topic artifact (`CU-855`) was found via the Jira text search, not this Confluence query.
- Confluence CQL `text ~ "omniauth-hubspot-1"` limit 40 → 4 pages, same 4 as the first Confluence query, none naming the "-1" suffix.
- Slack: `slack_search_public` query `"omniauth-hubspot"` (`response_format=concise`, `include_context=false`, limit 20) → 3 messages, all 2025-09 to 2026-05, all tooling-rollout/PR-list mentions (dependabot rollout, cleaner-action PR list, github-topics audit); none is early design discussion. No cursor remained (end of results).
- Datadog: MCP not available in this environment; no query performed.

## Identification

- Canonical name: `omniauth-hubspot-1`.
- Repo: `https://github.com/unbounce/omniauth-hubspot-1`, default branch `master`, visibility `PUBLIC`, not archived (`gh repo view`, above).
- This is a Ruby gem (an OmniAuth strategy library), not a deployed, running service. `entity.datadog.yaml` (added by PR #8, see Evolution) tags it `archetype:library` at the repo root — this is the repo's own nominal self-classification, not an observed runtime signal. No Datadog dashboards, traces, or registration were queried (tool unavailable); none would be expected for a library with no server process.
- Gem name inside the code is `omniauth-hubspot` (gemspec `gem.name = 'omniauth-hubspot'`, current version `0.1.1` per `lib/omniauth/hubspot/version.rb`) — distinct from the GitHub repo name `omniauth-hubspot-1`.
- GitHub repo topics (`repositoryTopics`, full list): `owner-platform-services`, `release-not-compliant`, `workflow-trunk`, `tier-5`. `owner-platform-services` is a nominal claim, not a documented ownership transfer.
- `entity.datadog.yaml` also declares `owner: platform-services` and `tier: "5"` — again a nominal self-declaration made by the same golden-path automation that wrote the file, not independent corroboration.
- No Confluence space or Jira project is dedicated to this repo. The one substantive originating ticket (`CU-855`) lives in project Customer, key `CU`. `PL` is a former key of that same project: `getJiraIssue PL-855` resolves to `CU-855`, the same issue id `20032`. HubSpot-integration work is not confined to `CU` — the 6 issues returned by `text ~ "omniauth-hubspot"` are `CU-123`, `CU-142`, `CU-855`, `CU-2068`, `CU-4715` and `BD-13175` ("HubSpot Marketplace Install Flow Partner Sign-in", project Build).
- Aliases encountered: the gem is installed elsewhere under the plain name `omniauth-hubspot` (see README/gemspec below); the upstream/original project is `chamaeleonidae/omniauth-hubspot` on GitHub.

## Origin

The initial commit (`6a9f511`, "Initial commit", 2017-06-08 16:50:05 -0700, author Brian Norton `brian.nort@gmail.com`) added a `.gitignore`, `Gemfile`, `README.md`, and `Rakefile` only — no library code. Two follow-up commits by the same author, `6aee2da` "Add oauth config and initial working version." (2017-06-08) and `8cdb3cf` "Use the access token information (hub_id etc.) to provide the uid and extra info." (2017-07-21), added the actual OmniAuth strategy code.

The repo's own README and gemspec self-identify a different canonical origin: the README's installation instructions read `gem 'omniauth-hubspot', git: 'https://github.com/chamaeleonidae/omniauth-hubspot'`, and the gemspec's `homepage` field is the same URL, with `gem.authors = ['Brian Norton']` and `gem.email = ['brian@trychameleon.com']` (both read at `origin/HEAD`, the README unchanged since the initial commit `6a9f511` (diffed against `origin/HEAD:README.md`); the gemspec did not exist at `6a9f511`, whose tree is four files, and was added 20 seconds later in `6aee2da` and is byte-identical since, which are byte-identical). `gh repo view chamaeleonidae/omniauth-hubspot` confirms that repository exists, is not archived, and was created on GitHub at `2017-06-08T23:51:07Z` — 62 seconds after this repo's initial-commit author timestamp converted to UTC (`2017-06-08 23:50:05 UTC`). Which of the two repositories is the true origin of the code is not established by any artifact read; the close timestamps and the README/gemspec's own self-reference to `chamaeleonidae/omniauth-hubspot` as the install source are the only evidence found.

`gh repo view unbounce/omniauth-hubspot-1 --json isFork,parent` returns `isFork: false, parent: null` — GitHub's own record shows this repo was not created via GitHub's native fork mechanism from any parent. `gh repo view` also gives this repo's own GitHub `createdAt` as `2022-10-14T22:56:16Z`, more than five years after the initial commit's author date — meaning the git history was pushed to this GitHub repository well after the commits were originally authored, consistent with an import of pre-existing history rather than organic development on GitHub from 2017 onward. No commit, PR, or ticket read explains that gap.

On the `-1` suffix: Jira ticket `CU-855` ("Update Hubspot and Constant Contact to work with new omniauth-oauth2", project Customer, reported by Kieran Cormack, created 2019-03-01) instructs, as an acceptance criterion, to "fork constantcontact2 and hubspot oauth gems" at two explicit GitHub URLs, one of which is `https://github.com/unbounce/omniauth-hubspot` — the plain name, with no `-1` suffix. `gh repo view unbounce/omniauth-hubspot` today returns `Could not resolve to a Repository`, so that exact URL no longer resolves. The current repo `unbounce/omniauth-hubspot-1` was created on GitHub in 2022, over three years after `CU-855`. No artifact found (commit, PR, ticket, or Confluence page) states what happened to an unsuffixed `unbounce/omniauth-hubspot` repo between 2019 and 2022, or why the current repo carries a `-1` suffix. This is left as an open question (see Known unknowns) rather than inferred.

`CU-855` also asks for the strategy to gain a `callback_url` method matching a specific code snippet. This repo's commit `c8ccec4` ("add missing callback_url", 2020-02-27, author Kieran Cormack — the same person who reported `CU-855`) adds exactly that method to `lib/omniauth/strategies/hubspot.rb`, and the ticket predates the commit by 363 days, so the ordering is consistent with the commit implementing that ticket's requirement. No commit message or PR links the two directly by ticket number, so this is a content-and-authorship correspondence, not a stated citation.

## Evolution (compact)

After `c8ccec4` (2020-02-27), there is a gap of no commits at all until `e6d1125` on 2026-04-16 — 2,240 days with no activity on this repo. All 17 commits from 2026-04-16 onward are Unbounce platform-tooling additions, not changes to the gem's behavior: the Agentic PR Reviewer workflow (PR #1), the org-wide Dependabot + Dependabot-agent rollout under ticket `PS-4701` (PRs #2–#3, later partly reverted by PR #5's `chore: remove dependabot-agent caller workflow`, whose commit body states the repo "has no non-agentic CI on its PRs" so the agent's pass/fail signal was fabricated), a CODEOWNERS stub (PR #4, later corrected by PR #9 because the original single-token file had zero parsed owners), a cleaner-workflow addition (PR #7), and a golden-path "manifest-distribute" codemod (PR #8, which added `entity.datadog.yaml`). No PR or commit in this window touches `lib/`, the gemspec version, or the README's technical content.

## Current state signals

- Last commit touching the gem's actual code: `c8ccec4`, 2020-02-27, Kieran Cormack. Last commit of any kind: `c3fea9d`, 2026-08-04, Braemel Thorn (CODEOWNERS routing fix, PR #9) — a tooling/config change, not gem code.
- Contributors, merged-identity counts from `git shortlog -sne origin/HEAD` (7 raw identities, merged by matching email): Braemel Thorn — 7 (across "Braemel" ×6 and "Braemel Thorn" ×1, same email `braemel.thorn@unbounce.com`); Genevieve Awa — 8 (across "genevieve-awa" ×6 and "Genevieve Awa" ×2, same email `genevieve.awa@unbounce.com`); Brian Norton — 3; Kieran Cormack — 1; `unbounce-ci` — 2. `unbounce-ci` is excluded from the human counts as a machine account, not as a GitHub App bot: `gh api users/unbounce-ci` returns `type: User` with `name: null`, and `gh pr list` returns `is_bot: false` for its PR, so it fails a `type: Bot` test. The basis for exclusion is the null `name` plus a uniformly automated history in this repo — its entire footprint is 2 commits (`ad068cb` "chore: add Dependabot configuration (PS-4701)" and `0356f54` "chore: add Dependabot agent workflow (PS-4701)", both 2026-04-16, email `ci@unbounce.com`) and 1 PR (#2, "chore: add Dependabot + agent workflow (PS-4701)"), all three the org-wide `PS-4701` rollout. 19 commits by named people, 2 by that machine account, 21 total.
- Recent active contributors (last 12 months, i.e. since 2025-08-26): Braemel Thorn and Genevieve Awa only, both exclusively on tooling/CI files (`.github/workflows/*`, `.github/dependabot.yml`, `CODEOWNERS`, `entity.datadog.yaml`) per the commit list above; neither touched `lib/`.
- Activity time series: of 8 merged PRs (`gh pr list`, above; PR #6 remains open/unmerged), 7 were authored by named people and 1 (PR #2) by the `unbounce-ci` machine account, excluded on the basis stated above. All 7 named-person merged PRs fall inside the trailing 12 months (2025-08-26 to 2026-08-26: PRs #1, #3, #4, #5, #7, #8, #9). Zero PRs of any kind exist before 2026-04-16 — the repo had no PR-based workflow until then, so the trailing-12-month/preceding-12-month ratio is 7/0 (undefined, not "accelerating" in the organic sense). Sweep-adjusted characterization: all 7 named-person merged PRs are platform-tooling rollout work (agentic reviewer, Dependabot rollout, CODEOWNERS, cleaner workflow, golden-path manifest), none is a functional change to the gem; service-specific (gem-code) activity in this window is 0 PRs and 0 commits.
- CI: three workflows are registered (`gh workflow list`): `Cleaner`, `.github/workflows/reviewer.yml`, `Dependabot Updates`. There is no test-suite CI. Of the 30 most-recent runs retained as of 2026-08-26 (`gh run list --limit 30`), 6 are `.github/workflows/reviewer.yml` runs and all 6 have `conclusion: failure` (one run each tied to PRs #4, #5, #6, #7, #8, #9 — the run for PR #1 has already fallen out of the 30-run retention window) — PRs #2 (merged 2026-04-21) and #3 (2026-04-24) also predate that floor, so three of the nine merged PRs have no retained reviewer run. Of the remaining 24 runs, one is the `.github/workflows/dependabot-agent.yml` run tied to PR #4 (`conclusion: failure`, 2026-05-12); the other 23 split into two exhaustive groups by ecosystem: 15 weekly `bundler`-ecosystem Dependabot update-check runs, all 15 with `conclusion: success`, and 8 weekly `github_actions`-ecosystem Dependabot update-check runs, all 8 with `conclusion: failure`.
- Cross-repo consumers: searched all 234 non-excluded, non-subject local clones at `origin/HEAD` with `git -C <repo> grep -lE 'omniauth-hubspot' origin/HEAD` (234 = 237 cached clone directories minus the archived `lp-deployer`, minus the duplicate clone `ub-tf-infrastructure-ps4844`, minus the subject repo itself). 4 repos matched, 10 files in total, each read at `origin/HEAD` with `git -C <repo> grep -nE 'omniauth-hubspot' origin/HEAD`. `lp-webapp` — a genuine consumer, 4 files: `Gemfile:98` and `Gemfile_rails_next:107` both declare `gem 'omniauth-hubspot', git: 'https://github.com/unbounce/omniauth-hubspot-1'`, and both corresponding `.lock` files pin `omniauth-hubspot (0.1.1)` at `Gemfile.lock:21` and `Gemfile_rails_next.lock:21` (each lock also records the git remote at line 18 and the bang requirement in its DEPENDENCIES section), matching this repo's own `version.rb`. `agentic-tools` — 2 files, neither a dependency declaration: `tools/reviewer-eval/repos.txt:89` (`unbounce/omniauth-hubspot-1`), a plain one-repo-per-line list for a reviewer-evaluation run, and `tools/reviewer-eval/context/deploy-scripts/deploy_reviewer.sh:35` (`"unbounce/omniauth-hubspot-1"`), a bash script whose `REPOS=(...)` array is the target list for pushing the agentic-reviewer workflow into repos. `paulw` — 1 file: `github-branch-protection/repos.txt:28` (`omniauth-hubspot-1`), a plain one-repo-per-line list for a branch-protection run. `golden-path` — 3 files: `allowlist.txt:27` (`omniauth-hubspot-1`), the fleet repo allowlist whose header states the scanner and every fleet campaign must refuse repos not listed in it; `manifests/omniauth-hubspot-1/manifest.yaml:4` and `:14` (`name: omniauth-hubspot-1`, `url: https://github.com/unbounce/omniauth-hubspot-1`), this repo's own generated golden-path manifest; and `manifests/properties.json:158`, the fleet-wide per-repo properties map, whose `omniauth-hubspot-1` entry holds `gp_archetype: library`, `gp_owner: platform-services`, `gp_status: active`, `gp_tier: "5"`. None of those 6 non-`lp-webapp` files is Ruby or a dependency declaration, so none is gem consumption. A Gemfile pin proves `lp-webapp` declares the dependency at the stated version; it does not by itself prove a live code path in `lp-webapp` still exercises the HubSpot OAuth flow today, which was not separately verified.
- Open PRs: 1 (`gh pr list`, above) — PR #6, "chore(deps): drop unused oauth2", opened 2026-05-22, not merged, sitting on branch `cleaner/unused-dependencies-oauth2-9ad99239eb`; its commit `b5420f0` is confirmed absent from `origin/HEAD` (`git merge-base --is-ancestor` → `NO`).
- Recent incidents: none found naming this repo specifically. Two Confluence incident/postmortem pages from 2019 (`2019-02-28 Cannot Create New HubSpot and Constant Contact Integrations`, space `EN`; `2019-03-06 HubSpot and Constant Contact Post Mortem`, space `EN`) — both returned by the Confluence title query listed under Sources queried — concern HubSpot/Constant Contact authentication breaking in `lp-webapp`, contemporaneous with `CU-855`'s 2019-03-01 fork request, but neither page names this repo or the `omniauth-hubspot` gem specifically in the text captured by the search summaries, so they are noted here as context rather than cited as this repo's own incident history.
- Datadog: MCP tool not available in this environment as of 2026-08-26; no query performed. Not expected to carry traffic/traces given the library classification above.

## Known unknowns

- Why the current repo is suffixed `-1` while the 2019 ticket `CU-855` referenced an unsuffixed `github.com/unbounce/omniauth-hubspot` URL that no longer resolves (`gh repo view unbounce/omniauth-hubspot` → not found; searched Jira `text ~ "omniauth-hubspot-1"` and Confluence `text ~ "omniauth-hubspot-1"`, 0 explanatory results in either).
- Why there is a 62-second gap between this repo's initial-commit author timestamp and `chamaeleonidae/omniauth-hubspot`'s GitHub creation timestamp, and consequently which of the two is the actual origin of the code (no artifact found stating either direction).
- What happened to this repo (or its unsuffixed predecessor) between the 2020-02-27 commit and its 2022-10-14 GitHub `createdAt`, and again in the 2,240-day commit gap between 2020-02-27 and 2026-04-16 (searched full commit log, all 9 PRs, Jira `text ~ "omniauth-hubspot"` and `"omniauth-hubspot-1"`, Confluence same two queries, and Slack `omniauth-hubspot` — no result from any source covers that period).

## Confidence assessment

The commit log, PR list, and workflow/run history are exact reads (`git log`/`gh pr list`/`gh run list` against `origin/HEAD` or the API directly), so the 2026-tooling-era timeline (PRs #1–#9, CI failures, the `entity.datadog.yaml` addition) is solidly grounded. The 2017–2020 origin story rests on one on-topic Jira ticket (`CU-855`) plus the repo's own README/gemspec text and GitHub-API timestamps; it is well-cited but does not fully resolve the `-1` naming question, which is left as an explicit unknown rather than guessed. Confluence and Slack searches for the service name returned nothing dedicated to this repo: no page or message read is about this repo itself, and the closest Confluence material the title query surfaced is the two 2019 space-`EN` `lp-webapp` incident records, which are contemporaneous context rather than this repo's own history. That absence is itself informative for a repo this small. The cross-repo consumer search is exhaustive over the 234-repo local corpus, and all 10 matching files across the 4 matching repos were read at `origin/HEAD`, leaving exactly one real consumer (`lp-webapp`) and 6 tooling-inventory references; it does not rule out consumers outside that corpus, since `gh search code` was not additionally run given the local corpus already resolved the one Ruby ecosystem match unambiguously by file content. No Datadog query was possible (tool unavailable), which is expected and not a gap for a library.
